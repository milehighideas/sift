# GitHub authentication context for the sift project
# Automatically switches to the milehighideas account when entering this directory
gh auth switch -u milehighideas 2>/dev/null

# Use the repo's shared git hooks (shellcheck pre-commit lint in .githooks/).
git config core.hooksPath .githooks 2>/dev/null
